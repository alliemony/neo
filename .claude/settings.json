{
  "mcpServers": {
    "flyctl": {
      "type": "stdio",
      "command": "/root/.fly/bin/flyctl",
      "args": ["mcp", "server"],
      "env": {
        "FLY_API_TOKEN": "${FLY_API_TOKEN}"
      }
    }
  }
}
